# Core — no external dependencies required for mock mode.
# Install the providers you have API keys for:

# HTTP API for frontend (optional)
flask>=3.0.0
flask-cors>=4.0.0

# OpenAI (gpt-4o)
openai>=1.30.0

# Anthropic (claude-3-5-sonnet)
anthropic>=0.25.0

# Google Gemini (gemini-2.0-flash)
google-generativeai>=0.5.0
